# Frontend -> Backend wiring (Create React App)
# Provide ONE of the following (API_BASE preferred). If empty, defaults to http://localhost:3001
REACT_APP_API_BASE=http://localhost:3001
# REACT_APP_BACKEND_URL=http://localhost:3001

# Optional (not required by current code, but reserved for platform consistency)
# REACT_APP_FRONTEND_URL=http://localhost:3000
# REACT_APP_WS_URL=ws://localhost:3001
